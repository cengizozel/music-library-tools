# intake pipeline + library_organizer
mutagen
requests
pyyaml

# tagger (beets + acoustic fingerprinting; fpcalc binary installed separately,
# see README "One-time setup")
beets
pyacoustid
beautifulsoup4

# tests
pytest
